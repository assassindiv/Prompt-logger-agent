# Prompt Logger Agent

A beginner AI agent that logs user prompts for future use.

## How it works:
- Takes your natural language prompt
- Logs it with a timestamp in `prompt_log.txt`
- Will be upgraded later to pass prompts to AI models like GPT-4

## Run it:
```bash
python main.py
